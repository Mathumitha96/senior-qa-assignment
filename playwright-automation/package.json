{
  "name": "senior-qa-assignment",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm i && npx playwright install",
    "test": "rm -rf test-results playwright-report && npx playwright test",
    "test:ui": "rm -rf test-results playwright-report && npx playwright test --ui",
    "report": "npx playwright show-report",
    "allure:report": "rm -rf allure-report && npx allure generate allure-results -o allure-report && npx allure open allure-report",
    "clean": "rm -rf test-results playwright-report allure-report",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,md}\""
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "allure-playwright": "^2.15.1",
    "prettier": "^3.0.0"
  }
}
